# EVcharge V2.1

本版正式執行「資料層與 UI 分離」：

- `data/market.json`：市場情報
- `data/pricing.json`：目前價格模型
- `data/operators.json`：業者基本資料
- `data/price_history.json`：歷史價格資料結構
- `css/style.css`：UI 樣式
- `js/app.js`：前端邏輯
- `scraper.py`：市場情報爬蟲，改為「保留舊資料、只更新新資料」，避免爬蟲失敗造成頁面清空

## GitHub Pages
目前仍可直接使用靜態 GitHub Pages，不需要後端。

## 重要資料註記
`pricing.json` 與 `price_history.json` 中延續了 V2 Prototype 的示範數值；正式對外使用前，請以各 CPO 官方費率、站點條件與可驗證日期建立真實 snapshot。不要把 prototype history 當成已驗證的市場歷史。

## 下一階段
1. 以官方來源建立 `pricing_snapshots.json`。
2. 增加站點資料與地圖。
3. 建立 CPO 個別情報頁。
4. 將 OCPP Log Analyzer 接 AI API。
5. 增加搜尋、SEO、Open Graph、favicon 與網站分析。
